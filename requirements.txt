# SUNO Clips — production requirements
# Render + Docker build command: pip install -r requirements-saas.txt
# This file is kept for tooling compatibility and points to the canonical file.

-r requirements-saas.txt
